{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",  <-- Make sure this matches the new file name
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"  <-- Make sure this matches the new file name
    }
  ]
}
